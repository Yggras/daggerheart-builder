# Supabase project configuration. Copy to .env and fill in from the Supabase dashboard
# (Project Settings → API). The anon/publishable key is safe to ship in the client bundle.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
